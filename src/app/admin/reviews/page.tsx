import { PageShell } from "@/components/shop/page-shell";

export default function AdminPage() {
  return <PageShell title="Admin reviews" description="CRUD-ready module integrated via Prisma and route handlers." />;
}
