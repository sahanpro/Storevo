import { PageShell } from "@/components/shop/page-shell";

export default function AdminPage() {
  return <PageShell title="Admin categories" description="CRUD-ready module integrated via Prisma and route handlers." />;
}
